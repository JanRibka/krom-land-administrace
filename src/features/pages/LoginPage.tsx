import Login from "features/login/Login";

import Box from "@mui/material/Box";

const LoginPage = () => {
  return (
    <Box component='main'>
      <Login />
    </Box>
  );
};

export default LoginPage;
