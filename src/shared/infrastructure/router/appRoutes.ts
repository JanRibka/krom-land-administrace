export type AppRoute = "/" | "/home" | "/actions" | "/gallery" | "/contact";

export const AppRoute = {
  Dashboard: "/" as AppRoute,
  Home: "/" as AppRoute,
  Actions: "/" as AppRoute,
  Gallery: "/" as AppRoute,
  Contact: "/" as AppRoute,
};
