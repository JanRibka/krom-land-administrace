import Box from "@mui/material/Box";
import { styled } from "@mui/material/styles";

const FeatureStyled = styled(Box)(({ theme }) => ({
  position: "relative",
  width: "100%",
  maxWidth: "100%",
}));

export default FeatureStyled;
