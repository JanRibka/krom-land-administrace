import Box from "@mui/material/Box";
import { styled } from "@mui/material/styles";

const ReservationsTableStyled = styled(Box)(({ theme }) => ({
  height: "500px",
}));

export default ReservationsTableStyled;
